# OpenAI API Key for AI-assisted scraping
# Get your key from: https://platform.openai.com/api-keys
# Free tier includes $5 credit
OPENAI_API_KEY=sk-your-api-key-here

# Enable AI-assisted parsing for complex tables
# Set to 'true' to use hybrid approach (DOM first, AI fallback)
# Set to 'false' to use only DOM parsing (default)
USE_AI_PARSING=false

# Force AI parsing for all URLs (useful for testing)
# Set to 'true' to always use AI instead of DOM parser
# Cost: ~$0.84 per full scrape (28 pages)
FORCE_AI=false
